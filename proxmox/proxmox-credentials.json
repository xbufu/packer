{
	"proxmox_host": "{{ vault `secret/data/proxmox` `host` }}",
	"proxmox_skip_tls_verify": "true",
	"proxmox_username": "{{ vault `secret/data/proxmox` `username` }}",
	"proxmox_password": "{{ vault `secret/data/proxmox` `password` }}"
}
